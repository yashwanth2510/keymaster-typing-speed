# KeyMaster — Typing Speed Test & Practice Studio

A typing speed test with **AI-powered practice passages**, weak-key drills, AI coaching, a full typing arcade, **real recorded mechanical keyboard sounds**, daily streaks, and a Buy Me a Coffee button. AI features are powered by the OpenRouter/Gemini APIs.

## 🚀 Try It Live

**Final site (full app with working AI):**

👉 **https://keymaster-typing-studio.onrender.com**

This is the recommended URL to share and test all features — the AI topic generator, weak-key drills and AI coach run through the Node backend on this host. No sign-up or API key needed.

Static host (GitHub Pages) — frontend-only mirror:

- https://yashwanth2510.github.io/keymaster-typing-speed/
- This copy has no backend, so AI features (AI Topic, weak-key drills, coaching) are unavailable there. Use the Render URL above for the full experience.

## ✨ Features

- **Speed Tests** — Time, Words, Quotes, Code and **AI Topic** modes with live WPM/accuracy tracking
- **AI Practice Passages** — enter any topic (e.g. "Cyberpunk Neo-Tokyo") and get custom text generated by AI
- **Weak Key Drills** — auto-detected from your error history, with varied AI-generated drills (guarded against repetitive output)
- **AI Typing Coach** — post-test feedback with actionable tips
- **Typing Arcade** — 4 games: Meteor Storm (planet defense), Speed Racer (drag race), Bubble Pop (deep-sea), Word Bricks (typing demolition) — all styled in the KeyMaster theme
- **Real Keyboard Sounds** — actual recorded mechanical switch sounds (Cherry MX Blue, Holy Panda, NK Cream, Kailh Turquoise) with press + release playback; profiles map to clicks, thocks and typewriter-style keys
- **Buy Me a Coffee** — support link (coffee icon with steam animation) in the header
- **Daily Streaks** — accurate consecutive local-day tracking in your own timezone (browser-only, no cloud store)
- **Tutorials** — structured row-based lessons with progress tracking
- **Stats Hub** — full result history, streaks, graphs and weak-key analysis

## 🧪 Test It

1. Open https://keymaster-typing-studio.onrender.com
2. Click the **AI Topic** tab in speed test mode → type any topic → **Generate**
3. Finish a test → click **Get Key Master Advice** for AI coaching

## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in `.env.local` to your Gemini API key:
   `Get-Content .env.example | Set-Content .env.local` (or copy manually)
   Then edit `.env.local` and replace `MY_GEMINI_API_KEY`.
3. Run the app:
   `npm run dev`

The app runs at http://localhost:3000.

## Deploy (GitHub repo + Render)

The app calls `/api/*` endpoints at the Node backend. AI generation supports either provider —
set `GEMINI_API_KEY` or `OPENROUTER_API_KEY` (drop any provider key in the OpenRouter field; a
free-tier Llama model is the default). The key powers AI topics, weak-key drills and coaching,
and falls back to built-in content only when no key is configured or a request fails. Deploy
the repo to **Render** (free tier) so it has a real backend — the frontend build is served from
the same host, so you only need this one link.

### Deploy to Render

1. Push this folder to a new GitHub repository:
   - `git init`
   - `git add .`
   - `git commit -m "initial commit"`
   - `git branch -M main`
   - `git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git`
   - `git push -u origin main`
2. Go to https://render.com and sign up (free).
3. Click **New > Blueprint** and connect your GitHub repo.
   Render detects `render.yaml` automatically and creates the web service.
4. In the Render dashboard, open the service → **Environment**, add:
   - Key: `GEMINI_API_KEY` — value: your Gemini API key
   (get one free at https://aistudio.google.com/apikey)
5. Render builds and deploys automatically. Every future `git push` redeploys.
6. Your app is live at the URL Render gives you (e.g. `https://keymaster-typing-studio.onrender.com`).

Deploys elsewhere (Railway, Cloud Run, Fly.io) also work the same way — `npm run build`, then
`NODE_ENV=production node dist/server.cjs`, with `GEMINI_API_KEY` set as an environment variable.

## Tech Stack

React 19 + Vite + TypeScript, Express server, OpenRouter/Gemini API (`@google/genai`), Recharts, Canvas Confetti, Web Audio (real key samples), TailwindCSS v4.